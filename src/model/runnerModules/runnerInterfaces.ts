type UpdateRunnerValuesArgs = {
  cursorXY: number[];
  isVertical: boolean;
  minMax: number[];
  isRange: boolean;
  fieldSize: number[];
  runner: RunnerModel[];
  thisRunner: RunnerModel;
};

export { UpdateRunnerValuesArgs };
